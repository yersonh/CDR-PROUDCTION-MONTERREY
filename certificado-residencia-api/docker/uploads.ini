; La imagen base php:8.4-cli-alpine no incluye php.ini, así que sin esto PHP
; usa sus valores por defecto (upload_max_filesize=2M, post_max_size=8M),
; muy por debajo del límite de 20 MB que la app declara para los soportes.
upload_max_filesize = 25M
post_max_size = 75M
